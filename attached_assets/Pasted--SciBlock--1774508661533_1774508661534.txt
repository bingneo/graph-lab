先不要接真实数据库。再做一轮“集成影响审计”，重点检查是否会影响 SciBlock 现有功能和代码结构
现在先不要连接我们服务器上的真实数据库。
也不要继续做新功能。
请再做一轮严格审计，这次重点不是数据库只读，而是：
如果现在把 Mechanism Graph 集成进 SciBlock，它是否会影响 SciBlock 现有任何功能、现有代码结构、现有数据传递链路。
我的要求非常明确：
不能影响 SciBlock 现有任何功能
不能破坏现有代码结构
不能污染现有数据流
不能影响本体信息的传递
不能影响现有实验记录模块、GraphLab 模块、标签系统、详情页、初始化流程
不能为了接入 Mechanism Graph 去改动已有业务模块的行为
一、这次只做“集成影响审计”，不要顺手改代码
请注意，这一步先做的是：
架构影响审查
路由影响审查
数据流影响审查
页面集成影响审查
状态管理影响审查
本体信息传递链路审查
调试代码隔离审查
不要先动代码。
如果发现问题，先列出问题和最小修复方案，不要直接改。
二、重点检查 1：Mechanism Graph 是否是“附加模块”，而不是“侵入式改造”
请检查当前实现方式，确认：
必须验证
Mechanism Graph 是否作为一个 独立附加模块 接入
它是否只是读取现有数据并构建自己的 view model
它是否没有反向要求现有模块为它改数据结构
它是否没有要求现有页面为它改变状态管理方式
它是否没有把 graph 相关逻辑散落到 SciBlock 其他核心模块里
我最关心的是
Mechanism Graph 必须是：
读取现有能力
消费现有数据
独立派生 graph 视图
而不是：
改造现有业务模块以适配它
把 graph 逻辑嵌入 SciBlock 主业务流程
让其他模块反过来为 graph 服务
请明确判断：
当前实现更接近 插件式附加模块，还是已经有 侵入式耦合风险。
三、重点检查 2：是否会影响本体信息传递链路
这个是重点中的重点。
请你专门检查当前 Mechanism Graph 对以下链路是否有影响：
要检查的内容
实验初始化时，本体信息的生成与修正链路
实验记录中本体信息的存储结构
本体信息在 GraphLab / 实验记录页 / 详情页之间的传递
Mechanism Graph 是否会改写、覆盖、拦截、重组这些本体信息
Mechanism Graph 的 diff / init dialog / block 聚合，是否只是 消费本体信息，而不是影响本体信息主链路
我要的结论
请明确告诉我：
Mechanism Graph 是否 只读取本体信息结果
是否完全不会影响现有本体信息生成和传递
是否存在任何“graph 视图模型反向污染本体链路”的风险
四、重点检查 3：是否会影响现有 GraphLab / 实验记录 / 详情页功能
请系统检查以下页面和模块的影响范围：
必须验证
GraphLab 原有 tab 和交互是否被改变
实验记录详情页原有逻辑是否被改变
现有 lineage / graph 相关功能是否被破坏
BlockDetailDialog 中的实验记录跳转接入后，是否会影响原有记录页行为
当前新增的 MechanismGraphPage 是否只是“新增能力”，不是“改写旧能力”
结论要求
请明确列出：
哪些现有文件被修改了
每个修改属于：
新增挂载
轻量接线
逻辑侵入
潜在风险点
我要知道现在这套实现，是否已经对原有 GraphLab 形成了过深耦合。
五、重点检查 4：是否会影响 SciBlock 现有代码结构
请站在工程结构角度检查，而不是只看功能能不能跑。
必须验证
Mechanism Graph 新增代码是否基本集中在自己的目录和层次中
是否保持了页面层、组件层、业务层、工具层分离
是否没有把大量特殊判断写入通用模块
是否没有让原有模块承担过多 graph 特化逻辑
当前集成方式是否符合“长期可维护、可扩展、可上线”的项目结构
我要的结论
请明确判断：
当前集成是否 符合可维护的附加模块接入方式
还是只是“暂时能跑，但后期会污染代码结构”
六、重点检查 5：调试 / mock / fixture 是否绝不会影响正式产品逻辑
之前为了开发这个模块，引入了：
debug panel
debug query parser
mock graph fixtures
请再次单独检查：
必须验证
这些调试能力是否只在 DEV 下可见
是否不会影响正式环境构建
是否不会影响真实数据链路
是否不会污染 SciBlock 正式代码路径
是否不会对现有功能造成隐藏副作用
我需要明确结论
请不要只说“应该没问题”，要明确告诉我：
生产环境下是否完全不可见
是否存在任何残留引用或结构污染风险
如果有低风险点，具体是什么
七、重点检查 6：如果现在集成进 SciBlock，是否需要改现有业务模块
这个问题请你明确回答。
我希望最终结论是：
Mechanism Graph 可以作为独立模块接入
不需要改现有业务模块的核心逻辑
不需要改现有本体链路
不需要改现有数据库结构
不需要改现有实验记录主流程
请你检查当前实现后，明确告诉我：
是否需要改这些东西
现有数据库 schema
现有实验记录写入流程
现有本体初始化流程
现有 GraphLab 主流程
现有实验记录详情页核心逻辑
如果需要，请列出来。
如果不需要，请明确写“不需要”。
八、我需要的最终交付：SciBlock 集成影响审计报告
请最后给我一份报告，至少包含：
1. 总结论
只能给出以下三种之一：
Safe to integrate：可以安全集成进 SciBlock
Conditionally safe：需要先修复若干结构问题后再集成
Not safe：当前不适合集成
2. 对现有功能影响
是否会影响现有功能
是否会影响本体信息传递
是否会影响实验记录链路
是否会影响 GraphLab 现有逻辑
3. 对现有代码结构影响
是否符合独立附加模块接入
是否存在侵入式耦合风险
4. 调试代码风险
是否完全隔离
是否有残留风险
5. 最终建议
现在是否可以集成
如果不能，最小修复项有哪些
九、边界要求
请严格遵守：
不要接真实数据库
不要改现有功能
不要顺手重构别的模块
不要为了通过审计去偷偷调整业务逻辑
先给我审计报告，我确认后再决定下一步。