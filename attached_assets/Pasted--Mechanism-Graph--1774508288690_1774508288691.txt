先做接真实数据库前的安全检查，不要直接改代码或接库
现在先不要直接连接我们服务器上的真实数据库，也不要先做新功能。
先对 Mechanism Graph 模块 做一次严格的接库前检查和只读安全验收。
这次检查的目标非常明确：
核心目标
确认 Mechanism Graph 模块是否已经具备连接真实数据库的条件，并重点验证以下 4 件事：
该模块是否只读数据库内容
点击实验记录列表，是否只会跳转到真实实验记录界面
该模块不会修改我们真实数据库中的任何数据
该模块不会影响我们现有任何功能、现有页面、现有代码逻辑
一、这次只做检查和验收，不要先改功能
请注意，这一步先做的是：
代码审查
数据流审查
API 路由审查
数据库访问审查
跳转链路审查
风险点清单
不要先动真实数据库
不要先改现有业务逻辑
不要先改已有页面功能
不要为了“顺手优化”去修改其他模块
如果发现问题，先列出问题和修复建议，不要直接大改。
二、重点检查 1：Mechanism Graph 是否严格只读
请你系统检查这个模块当前所有相关代码路径，确认：
要检查的内容
Mechanism Graph 相关 API 是否全部是 GET / 纯读取
后端是否存在任何：
INSERT
UPDATE
DELETE
UPSERT
migration
background write
缓存回写
审计写入
前端点击节点、点击弹窗、点击筛选、点击 compare mode 时，是否会触发任何写请求
是否有任何“为了生成 graph / 归档 block / 写审计记录”而落库的逻辑
结论要求
要明确告诉我：
当前 Mechanism Graph 模块是否 100% 只读
哪些文件参与了数据库读取
是否存在任何写操作风险
如果有风险点，具体在哪一层
三、重点检查 2：接真实数据库后，会不会动到我们现有数据
请从数据库访问层和服务层确认：
必须验证
当前 graph 构建是否只是读取已有实验记录数据
block 归档、阶段推断、diff 摘要、tag summary 是否全部是 内存中的派生计算
这些派生结果是否 不会回写数据库
当前代码中是否存在“将 graph 结果存表 / 回写字段 / 更新状态”的逻辑
接上真实数据库后，是否会触发任何 schema 变更或数据修正行为
我最关心的是
这个模块必须只是：
读取真实实验记录
在服务端/前端内存里计算 graph view model
返回页面展示
而不是：
改实验记录
改标签
改 block 归属
改状态
写 graph 表
写缓存表
四、重点检查 3：点击实验记录列表，是否能跳到真实实验记录界面
请检查这条链路是否已经安全可用：
要验证的链路
Block Detail Dialog 中的实验记录列表点击后
是否会跳到我们现在已有的 真实实验记录页面
跳转后是否会加载 真实记录内容
这个跳转是否只是读取，不会触发任何写入
是否会影响我们现有 GraphLab / 实验记录页 的既有逻辑
结论要求
请明确说明：
现在点击实验记录列表，实际跳转到了哪个页面 / 哪个路由
是否已经接的是现有真实实验记录页面
是否只是“读取并展示”
是否会改动现有记录页逻辑
五、重点检查 4：是否会影响我们已有任何代码和功能
这个非常重要。
请做一次影响面检查，确认当前 Mechanism Graph 模块：
必须检查
是否改动了现有实验记录模块的核心逻辑
是否改动了现有 GraphLab 原有功能行为
是否会影响已有标签系统
是否会影响已有实验记录详情页
是否会影响已有 API 行为
是否会影响已有数据库查询路径
是否引入了会污染生产逻辑的 debug / mock / fixture 代码
特别注意
之前为了开发和截图，有 debug panel / mock fixtures / query debug state。
请你明确检查：
这些调试能力是否 只在 DEV 环境生效
生产环境是否完全不会暴露
是否不会影响真实数据库接入
六、我需要的最终交付不是“已经接好”，而是“接库前验收报告”
请最后给我一份 Mechanism Graph 接真实数据库前验收报告，至少包含：
1. 数据读写安全结论
当前模块是否严格只读
是否存在任何写库行为
是否存在潜在写风险
2. 数据来源说明
现在 graph 用的真实字段 / 表 / JSON 数据来自哪里
哪些是直接读取
哪些是内存派生
3. 点击实验记录跳转说明
当前点击实验记录会跳到哪里
是否已经是现有真实实验记录页
是否只读
4. 对现有系统的影响范围
改了哪些现有文件
是否影响已有功能
是否有回归风险
5. 接真实数据库前是否已经 ready
请明确给结论，只能是以下三种之一：
Ready：可以安全连接真实数据库
Conditionally ready：需要先修复若干问题后再接
Not ready：当前还不能接真实数据库
6. 如果不是 Ready
请列出：
问题清单
风险等级
最小修复方案
七、这一步的边界
请严格遵守：
不要直接连接真实数据库
不要改数据库
不要改现有功能行为
不要顺手重构别的模块
不要为了通过检查去偷偷改业务逻辑
先把检查做完，给我报告，我确认后再决定下一步。