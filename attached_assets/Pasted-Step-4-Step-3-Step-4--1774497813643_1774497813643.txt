Step 4：把主链实线差异弹窗和起点初始化弹窗做成真实内容
Step 3 我先通过。
现在进入 Step 4。
这一步先不要继续加新玩法，重点是把当前主链上的两个关键弹窗从“占位框架”补成“真实内容”。
目前 compare dialog 已经是真实差异数据了，下一步要把：
MainTransitionDialog
InitTransitionDialog
也做成真正可用的内容。
一、目标
完成两类主链实线弹窗的真实内容接入：
主链相邻阶段差异弹窗
即 block -> block 的 main_transition
主链起点初始化弹窗
即 project -> first block 的初始化信息对比
要求：
不再显示占位文案
接真实数据
用结构化展示
保持和 compare dialog 同等级的产品完成度
二、主链相邻阶段差异弹窗要求
请把 MainTransitionDialog 做成真实的相邻 block 差异查看器。
数据来源
直接基于 source block 和 target block 背后的真实实验信息计算差异。
不要再用占位文本。
差异维度
至少仍按这 4 个维度展示：
实验系统
实验准备
实验操作
测量过程
展示方式
不要直接输出 JSON。
请做成结构化对比，风格尽量和 compare dialog 保持一致。
建议：
左侧：前一阶段 block
右侧：后一阶段 block
每个维度下展示：
仅前一阶段存在
共有
仅后一阶段存在
如果可以，允许再补一个“阶段推进摘要”，但这不是本步硬要求。
三、主链相邻阶段差异和 compare diff 的关系
请不要复制两套完全独立的 diff 逻辑。
最好抽出共用能力，例如：
block A / B → diff view model
compare dialog 和 main transition dialog 共用底层 diff 引擎
也就是说：
横向比较（虚线）
相邻推进比较（实线）
可以是不同 UI 入口，但底层 diff 能力应尽量复用。
四、主链起点初始化弹窗要求
请把 InitTransitionDialog 做成真实内容，而不是占位。
语义目标
这个弹窗表达的是：
初始化后的实验本体信息 vs 用户修正后的实验本体信息
也就是 project 起点到第一个 block 之间，到底发生了什么“定义层”的变化。
展示内容
请尽量接当前已有真实数据，至少做出一个可用 V1。
建议思路：
左侧：初始化抽取得到的实验信息
右侧：进入第一个 block 后实际保留 / 修正后的实验信息
仍按以下 4 个维度展示：
实验系统
实验准备
实验操作
测量过程
如果当前数据库里没有完整“初始化版本”
那不要继续占位，也不要卡住开发。请做一个明确可运行的 V1：
优先从当前已有初始化来源读取
如果缺失，就基于第一个 block 的最早 snapshot / 初始抽取结果做近似映射
但要在代码里明确哪些是真实初始化数据，哪些是合理 fallback
也就是说：
这一版允许有 fallback，但不允许继续只显示“后续版本提供”。
五、数据标注要求
对于 InitTransitionDialog，请在实现里明确区分：
真实初始化来源
fallback 推导来源
这样后面我们再补强时，不会混淆数据语义。
六、UI 要求
这一步两个弹窗都要达到“可直接演示给用户”的程度。
要求：
不再是空框架
有清晰标题
有阶段或对象上下文
内容可滚动
分组清晰
和 compare dialog 的风格尽量统一
七、代码结构要求
请尽量抽取共用逻辑，不要让三个 dialog 各自复制一套 diff 算法。
建议检查是否可以沉淀为：
block-to-block diff adapter
init-to-first-block diff adapter
共享的 diff section UI
这一步做完后，Mechanism Graph 的三种“查看差异”入口要尽量统一：
主链实线差异
主链起点初始化差异
任意两个 block compare 差异
八、这一步先不做的内容
这一步先不要做：
AI 自动生成自然语言差异总结
compare 结果持久化
多组 compare 历史
block 归档算法升级
graph 布局优化
复杂筛选器
先把“主链两类弹窗都变成真实内容”做完整。
九、本步交付要求
完成后请告诉我：
新增 / 修改了哪些文件
MainTransitionDialog 现在的真实数据流是怎样的
InitTransitionDialog 现在的数据来源是怎样的
哪些部分是真实初始化数据，哪些部分是 fallback
diff 逻辑是否和 compare dialog 共用了底层能力
当前页面是否已经能做到：
点击主链实线看真实相邻阶段差异
点击起点实线看真实初始化 vs 修正差异
TypeScript 是否零错误